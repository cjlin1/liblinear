# train

ADD_EXECUTABLE(train
    train.c
    )

TARGET_LINK_LIBRARIES(train
	${LINEAR_LIBRARIES}
    )

INSTALL(TARGETS train RUNTIME DESTINATION bin LIBRARY DESTINATION bin ARCHIVE DESTINATION bin)

