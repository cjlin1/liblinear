PROJECT(predict)

ADD_EXECUTABLE(predict
    predict.c
    )

TARGET_LINK_LIBRARIES(predict
	${LINEAR_LIBRARIES}
    )

INSTALL(TARGETS predict RUNTIME DESTINATION bin LIBRARY DESTINATION bin ARCHIVE DESTINATION bin)
